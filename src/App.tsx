export default function App() {
  return (
    <>
      {/* Since I moved the base route to the login page in router.tsx this page is unused, I just exported this because I didn't want to delete the file */}
      <h1>Empty App page</h1>
    </>
  );
}
